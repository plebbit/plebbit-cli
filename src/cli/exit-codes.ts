export enum exitMessages {
    ERROR_DAEMON_IS_DOWN = "Daemon is down. Please run 'plebbit daemon' before executing this command"
}

export enum exitCodes {
    ERROR_DAEMON_IS_DOWN = "ERROR_DAEMON_IS_DOWN"
}

// Bash exit status. A number from 1-255. If a command exits with status '0' then it has succeeded
export enum exitStatuses {
    ERROR_DAEMON_IS_DOWN = 1
}
